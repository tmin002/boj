{
    "configurations": [
    {
        "name": "lldb debug",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/output.bin",
        "cwd": "${workspaceFolder}/pwd",
        "preLaunchTask": "cbuild"
    }
    ]
}