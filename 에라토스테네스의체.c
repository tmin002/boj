#include <stdio.h>
#include <stdlib.h>

int main(void) {
	/*

	<에라토스테네스의 체에서 정수 n까지 소수를 구하려 할때, sqrt(n)보다 작은 수의 배수까지만 지우면 되는 이유>

	어떤 수의 자기 자신을 제외한 모든 약수들은 항상 그 수의 제곱근보다 작거나 같다. 따라서
	1부터 n까지에 존재하는 소수가 아닌 숫자들의 약수는 모두 1부터 sqrt(n)에만 존재한다.
	따라서 1부터 sqrt(n)까지의 소수들만 가지고 배수를 지운다고 해도, 1부터 n까지의 소수가 아닌 숫자들을 걸러낼 수 있는 것이다.
	*/


}
