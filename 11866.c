#include <stdio.h>
#include <stdlib.h>

int main(void) {
    int i, j, n, k, cnt;
    scanf("%d %d", &n, &k);

    int l[1000];
    for (i=0; i<n; i++)
        scanf("%d", &l[i]);

    i=0; cnt=0;
    for (; cnt<n; cnt++) {
        i = 
    }

}